<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="view_consignment_track_wizard_form" model="ir.ui.view">
            <field name="name">pharmacy.consignment.track.wizard.form</field>
            <field name="model">pharmacy.consignment.track.wizard</field>
            <field name="arch" type="xml">
                <form string="Track Consignment Stock">
                    <sheet>
                        <group>
                            <field name="purchase_order_id" />
                        </group>

                        <field name="line_ids" nolabel="1">
                            <list create="0" edit='0' delete="0">
                                <field name="product_id" />
                                <field name="received_qty" />
                                <field name="sold_qty" />
                                <field name="already_paid_qty" />
                                <field name="payable_now_qty" />
                                <field name="payable_remaining_qty" />
                                <field name="status" 
                                    widget="badge" 
                                    decoration-success="status == 'paid'" 
                                    decoration-warning="status == 'partial'" 
                                    decoration-info="status == 'pending'" />
                            </list>
                        </field>
                    </sheet>
                    <footer>
                        <button
                            name='action_create_payment_bill'
                            type='object'
                            string="Payment"
                            class="btn-primary" />
                        <button
                            string="Close"
                            special="cancel"
                            class="btn-secondary" />
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>